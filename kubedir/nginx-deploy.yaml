versionApi: extensions/v1beta
kind: Deployment
metadata:
   name: nginx-dep
spec:
# only run one template 
  replicas: 1
  template:
    metadata:
      labels:
      app: nginx-dep
    spec 
      containers:
       - name: nginx
         image: nginx:1.7.9
         ports:
           - containerPorts: 80